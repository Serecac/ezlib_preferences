allprojects {
    ext {
        versionMajor = 1
        versionMinor = 0
        versionPatch = 0
        nameApk = "Ezlib_Preferences"
        versionName = "${versionMajor}.${versionMinor}.${versionPatch}"
    }
}